kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4

containerdConfigPatches:
- |-
  [plugins."io.containerd.grpc.v1.cri".registry]
    config_path = "/etc/containerd/certs.d"

nodes:
  - role: control-plane
    extraMounts:
      - hostPath: ./containerd-config
        containerPath: /etc/containerd/certs.d
  - role: worker
    extraMounts:
      - hostPath: ./containerd-config
        containerPath: /etc/containerd/certs.d
  - role: worker
    extraMounts:
      - hostPath: ./containerd-config
        containerPath: /etc/containerd/certs.d
  - role: worker
    extraMounts:
      - hostPath: ./containerd-config
        containerPath: /etc/containerd/certs.d